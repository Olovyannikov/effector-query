import{_ as t,o as a,c as i,aa as o}from"./chunks/framework.vXJbu8_U.js";const g=JSON.parse('{"title":"Local development","description":"","frontmatter":{},"headers":[],"relativePath":"guide/contributing.md","filePath":"guide/contributing.md","lastUpdated":1781019088000}'),s={name:"guide/contributing.md"};function n(r,e,d,c,l,p){return a(),i("div",null,[...e[0]||(e[0]=[o(`<h1 id="local-development" tabindex="-1">Local development <a class="header-anchor" href="#local-development" aria-label="Permalink to &quot;Local development&quot;">​</a></h1><p>Want to hack on <strong>effector-refetch</strong> itself (not just use it)? Here&#39;s how to get the repo running.</p><h2 id="prerequisites" tabindex="-1">Prerequisites <a class="header-anchor" href="#prerequisites" aria-label="Permalink to &quot;Prerequisites&quot;">​</a></h2><ul><li><strong>Node ≥ 22.13</strong> (required by the pinned pnpm).</li><li><strong>pnpm 11.5.1</strong> — the repo pins it via <code>packageManager</code>. The easiest way is Corepack:</li></ul><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">corepack</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> enable</span></span></code></pre></div><h2 id="clone-install" tabindex="-1">Clone &amp; install <a class="header-anchor" href="#clone-install" aria-label="Permalink to &quot;Clone &amp; install&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/Olovyannikov/effector-query.git</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> effector-query</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span></code></pre></div><h2 id="scripts" tabindex="-1">Scripts <a class="header-anchor" href="#scripts" aria-label="Permalink to &quot;Scripts&quot;">​</a></h2><table tabindex="0"><thead><tr><th>script</th><th>what it does</th></tr></thead><tbody><tr><td><code>pnpm build</code></td><td>Build the library (<code>dist/</code>) — all entries + <code>.d.ts</code></td></tr><tr><td><code>pnpm typecheck</code></td><td><code>tsc --noEmit</code></td></tr><tr><td><code>pnpm lint</code></td><td>ESLint (incl. <code>eslint-plugin-effector</code>)</td></tr><tr><td><code>pnpm format</code></td><td>Prettier write (<code>format:check</code> to verify only)</td></tr><tr><td><code>pnpm test</code></td><td>Run the Vitest suite once (<code>test:watch</code> for watch)</td></tr><tr><td><code>pnpm size</code></td><td>Check bundle budgets (size-limit)</td></tr><tr><td><code>pnpm docs:dev</code></td><td>Run this documentation site locally</td></tr><tr><td><code>pnpm docs:build</code></td><td>Build the docs (also checks for dead links)</td></tr><tr><td><code>pnpm changeset</code></td><td>Record a changeset for your change (drives releases)</td></tr></tbody></table><h2 id="making-a-change" tabindex="-1">Making a change <a class="header-anchor" href="#making-a-change" aria-label="Permalink to &quot;Making a change&quot;">​</a></h2><ol><li>Branch off <code>main</code>.</li><li>Make the change with a test (the suite runs under <code>fork</code>/<code>allSettled</code> for scope-safety).</li><li>Run the gate: <code>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm test &amp;&amp; pnpm build</code>.</li><li>Add a changeset: <code>pnpm changeset</code> (pick <code>patch</code>/<code>minor</code>/<code>major</code> and write a line — it becomes the changelog entry).</li><li>Open a PR. On merge to <code>main</code>, CI opens a &quot;Version Packages&quot; PR; merging that publishes to npm.</li></ol><h2 id="trying-it-against-an-app" tabindex="-1">Trying it against an app <a class="header-anchor" href="#trying-it-against-an-app" aria-label="Permalink to &quot;Trying it against an app&quot;">​</a></h2><p>Run the examples directly from source (no build step) with <a href="https://github.com/privatenumber/tsx" target="_blank" rel="noreferrer">tsx</a>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tsx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/graphql.ts</span></span></code></pre></div><p>Or <code>pnpm build</code> and <code>pnpm pack</code> to get a tarball you can <code>pnpm add</code> into another project.</p><h2 id="pr-previews-canary-builds" tabindex="-1">PR previews &amp; canary builds <a class="header-anchor" href="#pr-previews-canary-builds" aria-label="Permalink to &quot;PR previews &amp; canary builds&quot;">​</a></h2><p>Every pull request gets two automatic stands:</p><ul><li><strong>Docs preview</strong> — the site is built and published to <code>gh-pages</code> under <code>…/pr-preview/pr-&lt;N&gt;/</code>; a bot comments the live URL on the PR (removed when it closes).</li><li><strong>Canary package</strong> — <a href="https://pkg.pr.new" target="_blank" rel="noreferrer">pkg.pr.new</a> publishes a preview build you can install straight from the PR: <code>npm i https://pkg.pr.new/Olovyannikov/effector-query@&lt;sha&gt;</code> (no npm pollution). The bot comments the exact command.</li></ul><p>So a reviewer can click the docs link and <code>npm i</code> the canary into a real app before merging.</p><h2 id="continuous-integration-github-actions" tabindex="-1">Continuous integration (GitHub Actions) <a class="header-anchor" href="#continuous-integration-github-actions" aria-label="Permalink to &quot;Continuous integration (GitHub Actions)&quot;">​</a></h2><p>Five workflows under <code>.github/workflows/</code>:</p><table tabindex="0"><thead><tr><th>Workflow</th><th>Trigger</th><th>What it does</th></tr></thead><tbody><tr><td><code>ci.yml</code></td><td>push / PR</td><td>typecheck · lint · test · build · size-limit</td></tr><tr><td><code>release.yml</code></td><td>push to <code>main</code></td><td>changesets: opens a &quot;Version Packages&quot; PR; on its merge, publishes to npm</td></tr><tr><td><code>docs.yml</code></td><td>push to <code>main</code></td><td>builds the docs and deploys them to the <code>gh-pages</code> branch (root)</td></tr><tr><td><code>pr-preview.yml</code></td><td>pull_request</td><td>builds the PR docs to <code>gh-pages</code> <code>pr-preview/pr-&lt;N&gt;/</code> + comments the URL</td></tr><tr><td><code>pkg-pr-new.yml</code></td><td>push / PR</td><td>publishes a canary via pkg.pr.new</td></tr></tbody></table><h3 id="required-repository-setup" tabindex="-1">Required repository setup <a class="header-anchor" href="#required-repository-setup" aria-label="Permalink to &quot;Required repository setup&quot;">​</a></h3><p>These need to be enabled once by a maintainer (Actions can&#39;t configure them itself):</p><ol><li><strong>Pages</strong> — Settings → Pages → Source: <strong>Deploy from a branch</strong> → <code>gh-pages</code> / <code>(root)</code>. (<code>docs.yml</code> deploys to that branch with <code>clean-exclude: pr-preview</code> so PR previews survive.)</li><li><strong>Workflow permissions</strong> — Settings → Actions → General → <strong>Read and write permissions</strong>, and <strong>Allow GitHub Actions to create and approve pull requests</strong> (for the Version Packages PR).</li><li><strong><code>NPM_TOKEN</code> secret</strong> — a classic <strong>Automation</strong> token (bypasses 2FA) or a Granular token with read+write on packages. Drives publishing in <code>release.yml</code>.</li><li><strong>pkg.pr.new GitHub App</strong> — install <a href="https://github.com/apps/pkg-pr-new" target="_blank" rel="noreferrer"><code>pkg-pr-new</code></a> on the repo so the canary workflow can publish (it&#39;s a no-op / non-blocking until then).</li></ol><p>The docs generators (<code>scripts/gen-api.mjs</code>, <code>scripts/gen-llms.mjs</code>) run inside <code>docs:build</code>, so both <code>docs.yml</code> and <code>pr-preview.yml</code> always ship a fresh API reference and <code>llms.txt</code>.</p>`,26)])])}const u=t(s,[["render",n]]);export{g as __pageData,u as default};
