import{_ as t,o as a,c as i,aa as o}from"./chunks/framework.vXJbu8_U.js";const g=JSON.parse('{"title":"Локальная разработка","description":"","frontmatter":{},"headers":[],"relativePath":"ru/guide/contributing.md","filePath":"ru/guide/contributing.md","lastUpdated":1781019088000}'),d={name:"ru/guide/contributing.md"};function s(r,e,n,c,l,p){return a(),i("div",null,[...e[0]||(e[0]=[o(`<h1 id="локальная-разработка" tabindex="-1">Локальная разработка <a class="header-anchor" href="#локальная-разработка" aria-label="Permalink to &quot;Локальная разработка&quot;">​</a></h1><p>Хотите дорабатывать сам <strong>effector-refetch</strong> (а не просто его использовать)? Вот как поднять репозиторий.</p><h2 id="требования" tabindex="-1">Требования <a class="header-anchor" href="#требования" aria-label="Permalink to &quot;Требования&quot;">​</a></h2><ul><li><strong>Node ≥ 22.13</strong> (требование закреплённого pnpm).</li><li><strong>pnpm 11.5.1</strong> — версия закреплена через <code>packageManager</code>. Проще всего через Corepack:</li></ul><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">corepack</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> enable</span></span></code></pre></div><h2 id="клонирование-и-установка" tabindex="-1">Клонирование и установка <a class="header-anchor" href="#клонирование-и-установка" aria-label="Permalink to &quot;Клонирование и установка&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">git</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> clone</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> https://github.com/Olovyannikov/effector-query.git</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> effector-query</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">pnpm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span></span></code></pre></div><h2 id="скрипты" tabindex="-1">Скрипты <a class="header-anchor" href="#скрипты" aria-label="Permalink to &quot;Скрипты&quot;">​</a></h2><table tabindex="0"><thead><tr><th>скрипт</th><th>что делает</th></tr></thead><tbody><tr><td><code>pnpm build</code></td><td>Сборка библиотеки (<code>dist/</code>) — все точки входа + <code>.d.ts</code></td></tr><tr><td><code>pnpm typecheck</code></td><td><code>tsc --noEmit</code></td></tr><tr><td><code>pnpm lint</code></td><td>ESLint (вкл. <code>eslint-plugin-effector</code>)</td></tr><tr><td><code>pnpm format</code></td><td>Prettier (запись; <code>format:check</code> — только проверка)</td></tr><tr><td><code>pnpm test</code></td><td>Прогон тестов Vitest (<code>test:watch</code> — в watch-режиме)</td></tr><tr><td><code>pnpm size</code></td><td>Проверка бюджета бандла (size-limit)</td></tr><tr><td><code>pnpm docs:dev</code></td><td>Запуск этой документации локально</td></tr><tr><td><code>pnpm docs:build</code></td><td>Сборка доки (заодно проверка битых ссылок)</td></tr><tr><td><code>pnpm changeset</code></td><td>Запись changeset для изменения (управляет релизами)</td></tr></tbody></table><h2 id="как-внести-изменение" tabindex="-1">Как внести изменение <a class="header-anchor" href="#как-внести-изменение" aria-label="Permalink to &quot;Как внести изменение&quot;">​</a></h2><ol><li>Ветка от <code>main</code>.</li><li>Изменение с тестом (набор гоняется под <code>fork</code>/<code>allSettled</code> ради scope-безопасности).</li><li>Прогоните гейт: <code>pnpm typecheck &amp;&amp; pnpm lint &amp;&amp; pnpm test &amp;&amp; pnpm build</code>.</li><li>Добавьте changeset: <code>pnpm changeset</code> (выберите <code>patch</code>/<code>minor</code>/<code>major</code> и напишите строку — она попадёт в changelog).</li><li>Откройте PR. При мерже в <code>main</code> CI открывает PR «Version Packages»; его мерж публикует в npm.</li></ol><h2 id="проверка-на-приложении" tabindex="-1">Проверка на приложении <a class="header-anchor" href="#проверка-на-приложении" aria-label="Permalink to &quot;Проверка на приложении&quot;">​</a></h2><p>Запускайте примеры прямо из исходников (без сборки) через <a href="https://github.com/privatenumber/tsx" target="_blank" rel="noreferrer">tsx</a>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> tsx</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> examples/graphql.ts</span></span></code></pre></div><p>Либо <code>pnpm build</code> и <code>pnpm pack</code> — получите tarball, который можно <code>pnpm add</code> в другой проект.</p><h2 id="pr-превью-и-канареика" tabindex="-1">PR-превью и канарейка <a class="header-anchor" href="#pr-превью-и-канареика" aria-label="Permalink to &quot;PR-превью и канарейка&quot;">​</a></h2><p>На каждый pull request автоматически поднимаются два стенда:</p><ul><li><strong>Превью доки</strong> — сайт собирается и публикуется в <code>gh-pages</code> под <code>…/pr-preview/pr-&lt;N&gt;/</code>; бот комментирует живой URL в PR (удаляется при закрытии).</li><li><strong>Канарейка пакета</strong> — <a href="https://pkg.pr.new" target="_blank" rel="noreferrer">pkg.pr.new</a> публикует превью-сборку, которую можно поставить прямо из PR: <code>npm i https://pkg.pr.new/Olovyannikov/effector-query@&lt;sha&gt;</code> (без засорения npm). Бот комментирует точную команду.</li></ul><p>Так ревьюер может открыть ссылку на доку и <code>npm i</code> канарейку в реальное приложение до мержа.</p><h2 id="continuous-integration-github-actions" tabindex="-1">Continuous integration (GitHub Actions) <a class="header-anchor" href="#continuous-integration-github-actions" aria-label="Permalink to &quot;Continuous integration (GitHub Actions)&quot;">​</a></h2><p>Пять воркфлоу в <code>.github/workflows/</code>:</p><table tabindex="0"><thead><tr><th>Воркфлоу</th><th>Триггер</th><th>Что делает</th></tr></thead><tbody><tr><td><code>ci.yml</code></td><td>push / PR</td><td>typecheck · lint · test · build · size-limit</td></tr><tr><td><code>release.yml</code></td><td>push в <code>main</code></td><td>changesets: открывает PR «Version Packages»; при его мерже публикует в npm</td></tr><tr><td><code>docs.yml</code></td><td>push в <code>main</code></td><td>собирает доку и деплоит её в ветку <code>gh-pages</code> (root)</td></tr><tr><td><code>pr-preview.yml</code></td><td>pull_request</td><td>собирает доку PR в <code>gh-pages</code> <code>pr-preview/pr-&lt;N&gt;/</code> + комментит URL</td></tr><tr><td><code>pkg-pr-new.yml</code></td><td>push / PR</td><td>публикует канарейку через pkg.pr.new</td></tr></tbody></table><h3 id="что-нужно-включить-в-репозитории-один-раз" tabindex="-1">Что нужно включить в репозитории один раз <a class="header-anchor" href="#что-нужно-включить-в-репозитории-один-раз" aria-label="Permalink to &quot;Что нужно включить в репозитории один раз&quot;">​</a></h3><p>Эти настройки включает мейнтейнер вручную (Actions сам их не настроит):</p><ol><li><strong>Pages</strong> — Settings → Pages → Source: <strong>Deploy from a branch</strong> → <code>gh-pages</code> / <code>(root)</code>. (<code>docs.yml</code> деплоит туда с <code>clean-exclude: pr-preview</code>, чтобы превью PR не затирались.)</li><li><strong>Права воркфлоу</strong> — Settings → Actions → General → <strong>Read and write permissions</strong> и <strong>Allow GitHub Actions to create and approve pull requests</strong> (для PR «Version Packages»).</li><li><strong>Секрет <code>NPM_TOKEN</code></strong> — классический <strong>Automation</strong>-токен (обходит 2FA) или Granular с правом read+write на пакеты. Управляет публикацией в <code>release.yml</code>.</li><li><strong>GitHub App pkg.pr.new</strong> — установите <a href="https://github.com/apps/pkg-pr-new" target="_blank" rel="noreferrer"><code>pkg-pr-new</code></a> на репозиторий, чтобы канарейка публиковалась (до этого шаг — no-op / non-blocking).</li></ol><p>Генераторы доки (<code>scripts/gen-api.mjs</code>, <code>scripts/gen-llms.mjs</code>) запускаются внутри <code>docs:build</code>, поэтому и <code>docs.yml</code>, и <code>pr-preview.yml</code> всегда отдают свежий API-референс и <code>llms.txt</code>.</p>`,26)])])}const k=t(d,[["render",s]]);export{g as __pageData,k as default};
